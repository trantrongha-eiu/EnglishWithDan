'use strict';
// Identical block shape to Writing Tips (overview/steps/list/callout/table/
// summary/lesson, plus the flexible example({label, lines:[{tag,text}],
// note}) shape) — Speaking content pairs things up the same varied way
// (Question/Answer, Band 6/7/8 samples, etc.), so just reuse it directly
// rather than re-defining the same helpers a third time.
module.exports = require('../writingTipsData/builder');
